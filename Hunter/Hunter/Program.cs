using System;
using UdemyHDL.LearningCS.VII.BaseDeDatos;

namespace UdemyHDL
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Seccion 2 Introduccion a C# */

            //// variables
            //Variable output = new Variable();

            //// Tipos de Datos
            //TiposDeDato output = new TiposDeDato();

            //output.OutPut();

            //// Funciones
            //Funciones output = new Funciones();

            //int salida = output.OutPut(4,3);

            //Console.WriteLine(salida);

            //// Sentencias If ElseIf
            //SentenciaIfElseIf output = new SentenciaIfElseIf();

            //// Sentencias Switch Case
            //SwitchCase output = new SwitchCase();

            //// Arreglos
            //Arreglos output = new Arreglos();

            //// While, Do While
            //While output = new While();

            //// For
            //For output = new For();

            /* Seccion 2 Introduccion a C# */

            //// Clases y Objetos
            //ClasesYObjetos output = new ClasesYObjetos();

            //// Propiedades
            //Propiedades output = new Propiedades();

            //// Herencia
            //Herencia output = new Herencia();

            //// Sobrecarga
            //Sobrecarga output = new Sobrecarga();

            //// Sobreescritura
            //Sobreescritura output = new Sobreescritura();

            //// Static
            //Static output = new Static();

            //// Inteface
            //Interface output = new Interface();

            //// Genericos
            //Generic output = new Generic();

            //// Json
            //// Serializacion de objetos (Json)
            //Json output = new Json();

            //// Lists
            //Lists output = new Lists();

            //// ForEach
            //ForEach output = new ForEach();

            //// List Common Methods
            //ListCommonMethods output = new ListCommonMethods();

            //// Tipos Anonimos
            //TiposAnonimos output = new TiposAnonimos();

            //// Tuplas
            //Tuplas output = new Tuplas();

            //// Excepciones
            //Excepciones output = new Excepciones();

            //// Excepciones Personalizadas
            //ExcepcionesPersonalizadas output = new ExcepcionesPersonalizadas();

            //// LINQ
            //LINQ output = new LINQ();

            //// LINQ Join
            //LINQ_Join output = new LINQ_Join();

            // Conexion a una BD
            BaseDeDatos output = new BaseDeDatos();

            output.OutPut();

        }
    }
}
